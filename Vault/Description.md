# Description

**Termmy** is a python package aiming to support GUI utilities in the terminal environment across various platforms. Being a project for fun, it uses **only the python standard library.** It centers around using text as pixels and optionally using ansi escape code for displaying 24bit colors.

Termmy aims to provide lowest overhead as reasonably possible while maintaining compatibility across different platforms and terminal emulators.